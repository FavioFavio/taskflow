# Backend

This folder is reserved for a future dedicated backend service.

For the current MVP, the application uses Next.js server-side code and Supabase from the `frontend` app. Add a Spring Boot project here only when the app needs backend responsibilities that should live outside Next.js, such as complex domain logic, private integrations, background jobs, or a REST API consumed by multiple clients.
